# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=satori
DB_USER=postgres
DB_PASSWORD=password

# Docker container name (if using Docker connection)
DOCKER_CONTAINER_NAME=satori-postgres

# Import settings
DEFAULT_CENTER=Nepal
DEFAULT_PERSON_TYPE=sangha_member
IMPORT_USER_ID=csv_import_script